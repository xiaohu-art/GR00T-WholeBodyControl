# 触觉皮肤衣集成进 gear_sonic 数据采集

## Context

用户要让 Unitree G1 穿矩侨(JuQiao)触觉皮肤衣执行遥操任务,把触觉信号作为 **observation** 写入 LeRobot 格式数据集,供下游 VLA 策略训练使用。

两边现状:
- **JuQiao**(`/data/humanoid-vla/GR00T-WholeBodyControl/JuQiao/`):串口采集,256 路 raw + 16 字节 gyro,**只输出 CSV/JSONL 文件,没有任何 IPC 接口**。可复用 `FrameParser`、`SampleAssembler`、`Sample` 和 `calibrate()`。
- **gear_sonic**(`/data/humanoid-vla/GR00T-WholeBodyControl/gear_sonic/`):多进程 + ZMQ 架构,`run_data_exporter.py` 作为唯一同步点,50 Hz 主循环从各 ZMQ topic 拉**最新一帧**(CONFLATE 语义),容忍 ≤100ms 老旧;时间戳用 `time.time()` 墙钟,各源在 ingest 时打戳。

用户确认的决策:
1. 只记 **256 路 raw 通道**(不记 gyro、不记 region 重排)
2. 作为 **observation**(`observation.tactile_raw`),策略训练时可见
3. **集成进 launcher**,launch_data_collection.py 加新 pane 自动起触觉进程
4. **每次启动重新校准**(100 帧零点,期间不发数据)

## 总体架构

沿用现有 ZMQ pub-sub 模式,**不**把串口 IO 塞进 exporter 主循环(避免阻塞 50 Hz 关键回路):

```
┌────────────────────────────────────┐
│ tactile_publisher.py (NEW)         │
│ - 串口 + FrameParser + Assembler   │
│ - calibrate() 启动时 100 帧        │
│ - ZMQ PUB tcp://*:5558 topic=tactile│
│ - 每帧发布 multipart:              │
│   [b"tactile", msgpack_header,     │
│    bytes(256_calibrated_raw)]      │
└──────────────┬─────────────────────┘
               │ tcp://localhost:5558
┌──────────────▼─────────────────────┐
│ run_data_exporter.py (modify)      │
│ - 新增 SUB socket on 5558          │
│ - _poll_tactile_zmq() 每 tick 调用 │
│ - latest_tactile_msg 缓存最新一帧  │
│ - _add_data_frame_sonic 写入       │
│   frame_data["observation.tactile_ │
│   raw"]                             │
└────────────────────────────────────┘
```

**时间同步**:发布端用 `time.time()`(墙钟,与 exporter 同源),exporter 取最新一帧,>100ms 老旧则填零(与 SMPL 一致策略,避免与坏数据训练)。

## 需要修改/新建的文件

### 1. **新建** `/data/humanoid-vla/GR00T-WholeBodyControl/JuQiao/scripts/tactile_publisher.py`

新的 ZMQ 发布进程,职责:
- CLI 参数:`--port /dev/ttyACM0`,`--baud 921600`,`--zmq-host 0.0.0.0`,`--zmq-port 5558`,`--calibration-samples 100`,`--topic tactile`
- **复用现有代码**(不重写协议):
  - 从 `jq_tactile_skin.protocol` 导入 `FrameParser`, `SampleAssembler`, `Sample`(JuQiao/jq_tactile_skin/protocol.py:42-114)
  - 从 `scripts/collect_serial.py` 复用 `read_next_wb_sample`, `calibrate`, `apply_calibration`(collect_serial.py:52-91)—— 提取成共享辅助模块更干净,或直接 `from collect_serial import ...`
- 主循环:
  ```python
  with serial.Serial(port, baud, timeout=0.2) as ser:
      baseline = calibrate(ser, 100)   # 复用
      ctx = zmq.Context()
      sock = ctx.socket(zmq.PUB)
      sock.bind(f"tcp://{zmq_host}:{zmq_port}")
      time.sleep(0.5)  # PUB 等订阅者 warm-up
      parser = FrameParser()
      assembler = SampleAssembler()
      while True:
          sample = read_next_wb_sample(ser, parser, assembler)
          calibrated = apply_calibration(sample.raw, baseline)
          host_time = time.time()
          header = msgpack.packb({
              "host_time": host_time,
              "packet1_time": sample.packet1_time,
              "packet2_time": sample.packet2_time,
              "shape": [256],
              "dtype": "uint8",
              "calibrated": True,
          })
          payload = bytes(calibrated)  # 256 bytes
          sock.send_multipart([b"tactile", header, payload], flags=zmq.DONTWAIT)
  ```
- 关键点:**校准期间不发布**(用户决策),100 帧静止采集后才进入发布循环。
- 退出处理:Ctrl-C → 关 socket、关 serial。
- 依赖:`pyserial`、`pyzmq`、`msgpack`(JuQiao 的 requirements.txt 需加 `pyzmq>=25` 和 `msgpack>=1.0`)。

### 2. **修改** `/data/humanoid-vla/GR00T-WholeBodyControl/gear_sonic/data/features_sonic_vla.py`

在 `get_features_sonic_vla()` 返回字典里新增条目(参照 wrist camera 的可选模式,line 372-385):

```python
def get_tactile_features() -> dict:
    """Features for optional JuQiao tactile skin suit (added when --record-tactile)."""
    return {
        "observation.tactile_raw": {
            "dtype": "uint8",
            "shape": (256,),
            "names": [f"raw_{i:03d}" for i in range(1, 257)],
        },
    }


def get_tactile_modality_config() -> dict:
    """Modality config entry for tactile."""
    return {
        "tactile": {
            "tactile_raw": {"original_key": "observation.tactile_raw"},
        },
    }
```

**不写进** `get_features_sonic_vla()` 主字典—保持可选,跟着 `--record-tactile` 标志条件添加(对应 run_data_exporter.py:917-925 的 wrist camera 模式)。

### 3. **修改** `/data/humanoid-vla/GR00T-WholeBodyControl/gear_sonic/scripts/run_data_exporter.py`

四处改动:

**(a) CLI 配置**(参考 line 83-93 处 sonic/state ZMQ 配置):
```python
tactile_zmq_host: str = "localhost"
tactile_zmq_port: int = 5558
record_tactile: bool = False  # 跟随 --record-tactile 启用
tactile_max_age_sec: float = 0.1  # 100ms 容忍,与 SMPL 一致
```

**(b) `GrootDataCollector.__init__`**(参考 line 258-275 sonic_zmq 初始化):
```python
self.latest_tactile_msg = None
self._tactile_zmq_ctx = None
self._tactile_zmq_socket = None
if record_tactile:
    try:
        self._tactile_zmq_ctx = zmq.Context()
        self._tactile_zmq_socket = self._tactile_zmq_ctx.socket(zmq.SUB)
        self._tactile_zmq_socket.connect(f"tcp://{tactile_zmq_host}:{tactile_zmq_port}")
        self._tactile_zmq_socket.setsockopt(zmq.RCVTIMEO, 100)
        self._tactile_zmq_socket.setsockopt(zmq.CONFLATE, 1)  # 只要最新
        self._tactile_zmq_socket.setsockopt_string(zmq.SUBSCRIBE, "tactile")
        print(f"[Tactile] Subscribed to {tactile_zmq_host}:{tactile_zmq_port}")
    except Exception as e:
        print(f"[Tactile] Warning: failed to init ZMQ subscriber: {e}")
        self._tactile_zmq_socket = None
```
> 注:这里 CONFLATE=1 是与 sonic_zmq 设置(=0)不同的取值;触觉发布频率可能 100-200Hz,exporter 50Hz,只关心最新即可。

**(c) 新增 `_poll_tactile_zmq()` 方法**(参考 line 337-347 `_poll_sonic_zmq_messages` 模式):
```python
def _poll_tactile_zmq(self):
    if self._tactile_zmq_socket is None:
        return
    try:
        parts = self._tactile_zmq_socket.recv_multipart(zmq.NOBLOCK)
    except zmq.Again:
        return
    if len(parts) != 3 or parts[0] != b"tactile":
        return
    header = msgpack.unpackb(parts[1])
    payload = np.frombuffer(parts[2], dtype=np.uint8)
    if payload.shape != (256,):
        return
    self.latest_tactile_msg = {
        "receive_timestamp": time.time(),
        "host_time": header["host_time"],
        "raw": payload,
    }
```
在主循环里调用:line 868-871 附近,`self._poll_state_zmq(); self._poll_sonic_zmq_messages();` 后面加 `self._poll_tactile_zmq()`。

**(d) `_add_data_frame_sonic`**(参考 line 673-703 SMPL 处理逻辑):
```python
# 容忍 100ms 老旧,否则填零
tactile_msg = self.latest_tactile_msg
if tactile_msg is None or (
    time.time() - tactile_msg["receive_timestamp"] > self.tactile_max_age_sec
):
    frame_data["observation.tactile_raw"] = np.zeros(256, dtype=np.uint8)
else:
    frame_data["observation.tactile_raw"] = tactile_msg["raw"]
```

**(e) features 注册**(参考 line 917-925 wrist camera 条件添加):
```python
if config.record_tactile:
    features.update(get_tactile_features())
    modality_config.update(get_tactile_modality_config())
```

**(f) 清理资源**(line 847-853 的 socket/ctx 关闭循环里把 tactile 加进去)。

### 4. **修改** `/data/humanoid-vla/GR00T-WholeBodyControl/gear_sonic/scripts/launch_data_collection.py`

**(a)** `DataCollectionLaunchConfig` 新增字段(参考 line 138 `record_wrist_cameras`):
```python
record_tactile: bool = False
"""Enable JuQiao tactile skin suit publisher and recording."""

tactile_port: str = "/dev/ttyACM0"
"""Serial port for tactile suit."""

tactile_baud: int = 921600
"""Baud rate for tactile suit serial connection."""

tactile_zmq_port: int = 5558
"""ZMQ port for tactile publisher."""
```

**(b)** tmux 布局升级(line 207-251 的 `_create_tmux_session`):  
当前是 2×2 = 4 pane;触觉如果启用,需要再多一个 pane。可以走两种方案:
- **新窗口** `tactile`(参考 sim 窗口模式,line 302-324):干净不挤,推荐。
- **当前窗口再拆分**:布局复杂、操作不直观。

**采用新窗口方案**(若 `config.record_tactile`):
```python
if config.record_tactile:
    subprocess.run(["tmux", "new-window", "-t", SESSION_NAME, "-n", "tactile"])
    tactile_cmd = (
        f"cd {repo_root / 'JuQiao'} && "
        f"python3 -m pip install -q -r requirements.txt && "  # 确保 pyzmq/msgpack
        f"python3 scripts/tactile_publisher.py "
        f"--port {config.tactile_port} "
        f"--baud {config.tactile_baud} "
        f"--zmq-port {config.tactile_zmq_port}"
    )
    subprocess.run(["tmux", "send-keys", "-t", f"{SESSION_NAME}:tactile",
                    tactile_cmd, "C-m"])
    time.sleep(2.0)
    # 切回 data_collection 窗口
    subprocess.run(["tmux", "select-window", "-t", f"{SESSION_NAME}:data_collection"])
```

**(c)** 把 `--record-tactile` 和 `--tactile-zmq-port` 透传给 data exporter(line 382-397 区域):
```python
if config.record_tactile:
    exporter_cmd += f" --record-tactile --tactile-zmq-port {config.tactile_zmq_port}"
```

**(d)** Banner 输出加一行 `Tactile: Yes/No`(line 283-297)。

### 5. **小幅修改** `/data/humanoid-vla/GR00T-WholeBodyControl/JuQiao/requirements.txt`

新增 `pyzmq>=25.0`、`msgpack>=1.0`(发布器依赖)。注意 JuQiao 用的是独立 conda env(README 说 `conda activate juqiao`),需要在那个 env 里装。launcher 里的 `pip install -r requirements.txt` 是一个保险措施。

## 时间同步与容错策略(详细)

| 时间属性 | 处理方式 |
|---------|---------|
| **同步基准** | `time.time()` 墙钟,发布端与 exporter 同源 |
| **每帧时间戳** | 发布端在收完 packet2 后调 `time.time()`,写入 msgpack header 的 `host_time` |
| **延迟容忍** | exporter 每帧检查 `time.time() - receive_timestamp < 100ms`,超时填零 256 个 0 |
| **触觉率 > exporter 率** | ZMQ CONFLATE=1,exporter 自然只取最新一帧;无需在发布端降采样 |
| **触觉率 < exporter 率** | 在容忍范围内会重复用同一帧(stale 但 fresh enough);超时则零填充 |
| **校准期间 (~1-2s)** | 发布器不发布任何数据,exporter 全部填零(数据集开头几秒不要按 record) |
| **触觉进程崩溃** | exporter 会一直填零,继续采集;事后通过 parquet 里的全零行可识别坏区段 |

> 不增加 sync barrier 或握手机制,保持与现有 SMPL/planner 一致的"宽松对齐"风格。

## 验证(端到端)

完成后用以下步骤验证:

1. **触觉发布独立测试**(不开整套):
   ```bash
   cd /data/humanoid-vla/GR00T-WholeBodyControl/JuQiao
   python3 scripts/tactile_publisher.py --port /dev/ttyACM0 --zmq-port 5558
   # 另一个终端
   python3 -c "
   import zmq, msgpack
   ctx = zmq.Context(); s = ctx.socket(zmq.SUB)
   s.connect('tcp://localhost:5558'); s.subscribe(b'tactile')
   for _ in range(20):
       t, h, p = s.recv_multipart()
       print(msgpack.unpackb(h), len(p))
   "
   ```
   期望:校准 ~2s 后,~100-200Hz 输出 256 字节 payload。

2. **不开真触觉硬件的 mock 测试**(可选):写一个 mock_tactile_publisher.py,产生随机 256 字节,跑 exporter 看 parquet 是否有 `observation.tactile_raw` 列。

3. **完整启动**:
   ```bash
   python gear_sonic/scripts/launch_data_collection.py \
       --record-tactile \
       --tactile-port /dev/ttyACM0
   ```
   tmux 应有新 `tactile` 窗口,触觉发布进程正常运行。data exporter pane 应出现 `[Tactile] Subscribed to ...` 日志。

4. **录一段数据,检查 parquet**:
   ```python
   import pandas as pd
   df = pd.read_parquet("outputs/<latest>/data/chunk-000/episode_000000.parquet")
   col = "observation.tactile_raw"
   print(col in df.columns, df[col].iloc[0].shape, df[col].iloc[100])
   ```
   期望:列存在、shape (256,)、有非全零的样本(说明触觉信号成功落盘且与机器人状态对齐)。

5. **时间对齐抽查**:对比 `observation.state`(关节角)和 `observation.tactile_raw` 的时间戳列;两者应在同一 episode 行号下,差值 <100ms。

## 注意事项 / 风险

- **串口设备名**:`/dev/ttyACM0` 可能因 USB 插拔顺序变化;生产用建议改用 udev rule 创建固定 symlink(`/dev/jq_tactile`)。
- **权限**:用户需在 `dialout` 组里,否则发布器开串口会被拒(JuQiao README 已说明)。
- **校准失败检测**:目前 `calibrate()` 没有"传感器全零/无响应"检测,若设备没插好会一直阻塞读 4096 字节;建议给 `read_next_wb_sample` 加个最大等待时间,超时报错退出而不是默默阻塞。这是可选的小改进,**不在本次范围**。
- **pyzmq 在 JuQiao venv**:JuQiao 现在 requirements 只有 pyserial,需要追加。
- **conda env 切换**:launcher 用的是 `.venv_data_collection` 等,JuQiao 用 conda `juqiao`;新 pane 必须显式 `conda activate juqiao && python3 scripts/tactile_publisher.py`,或者把 publisher 迁到 `.venv_data_collection`(需先在那里装 pyserial)。**待用户确认**这点。

## 不做的事

- 不动 JuQiao 现有 `collect_serial.py`(保留作为离线采集工具)
- 不重写协议层、不重写校准逻辑(直接复用)
- 不引入 ROS、共享内存、其他中间件
- 不改 SMPL/planner/state ZMQ 任何代码
- 不试图把触觉送进 C++ deploy(它是控制器,触觉是给数据集用的)
- 不做触觉数据可视化(JuQiao 自带 visualize_wb.py,仍可独立离线用)
